ACE = { # Aceh

	# Sumatra
	dynamic_country_name = {
		name = dyn_c_ace_union
		adjective = dyn_c_ace_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_ACEH
				owns_entire_state_region = STATE_NORTH_SUMATRA
				owns_entire_state_region = STATE_SOUTH_SUMATRA
			}
		}
	}
}

BLG = { # Bulungan

	# Kalimantan
	dynamic_country_name = {
		name = dyn_c_blg_union
		adjective = dyn_c_blg_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_WEST_BORNEO
				owns_entire_state_region = STATE_EAST_BORNEO
				owns_entire_state_region = STATE_NORTH_BORNEO
			}
		}
	}
}

BNJ = { # Banjar

	# Kalimantan
	dynamic_country_name = { # Kalimantan
		name = dyn_c_blg_union
		adjective = dyn_c_blg_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_WEST_BORNEO
				owns_entire_state_region = STATE_EAST_BORNEO
				owns_entire_state_region = STATE_NORTH_BORNEO
			}
		}
	}
}

IDN = { # Indonesia

	# Majapahit
	dynamic_country_name = {
		name = dyn_c_idn_monarchy_javan
		adjective = dyn_c_majapahit_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				has_law = law_type:law_monarchy
				ruler = {
					has_culture = cu:javan
				}
			}
		}
	}

	# Srivijaya
	dynamic_country_name = { # Srivijaya
		name = dyn_c_idn_monarchy_malay
		adjective = dyn_c_idn_monarchy_malay_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				has_law = law_type:law_monarchy
				ruler = {
					OR = {
						has_culture = cu:malay
						has_culture = cu:sumatran
					}
				}
			}
		}
	}
}

JMB = { # Jambi

	# Sumatra
	dynamic_country_name = {
		name = dyn_c_ace_union
		adjective = dyn_c_ace_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_ACEH
				owns_entire_state_region = STATE_NORTH_SUMATRA
				owns_entire_state_region = STATE_SOUTH_SUMATRA
			}
		}
	}
}

KTI = { # Kutai

	# Kalimantan
	dynamic_country_name = {
		name = dyn_c_blg_union
		adjective = dyn_c_blg_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_WEST_BORNEO
				owns_entire_state_region = STATE_EAST_BORNEO
				owns_entire_state_region = STATE_NORTH_BORNEO
			}
		}
	}
}

LAN = { # Lanfang

	# Bo-Ni
	dynamic_country_name = {
		name = dyn_c_lan_union
		adjective = dyn_c_boni_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_WEST_BORNEO
				owns_entire_state_region = STATE_EAST_BORNEO
				owns_entire_state_region = STATE_NORTH_BORNEO
			}
		}
	}
}

PON = { # Pontianak

	# Kalimantan
	dynamic_country_name = {
		name = dyn_c_blg_union
		adjective = dyn_c_blg_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_WEST_BORNEO
				owns_entire_state_region = STATE_EAST_BORNEO
				owns_entire_state_region = STATE_NORTH_BORNEO
			}
		}
	}
}

SAK = { # Siak

	# Sumatra
	dynamic_country_name = {
		name = dyn_c_ace_union
		adjective = dyn_c_ace_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_ACEH
				owns_entire_state_region = STATE_NORTH_SUMATRA
				owns_entire_state_region = STATE_SOUTH_SUMATRA
			}
		}
	}
}

SMB = { # Sambas

	# Kalimantan
	dynamic_country_name = {
		name = dyn_c_blg_union
		adjective = dyn_c_blg_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_WEST_BORNEO
				owns_entire_state_region = STATE_EAST_BORNEO
				owns_entire_state_region = STATE_NORTH_BORNEO
			}
		}
	}
}

SRK = { # Surakarta

	# Mataram
	dynamic_country_name = {
		name = dyn_c_srk_union
		adjective = dyn_c_srk_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_CENTRAL_JAVA
			}
		}
	}
}

STG = { # Sintang

	# Kalimantan
	dynamic_country_name = {
		name = dyn_c_blg_union
		adjective = dyn_c_blg_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_WEST_BORNEO
				owns_entire_state_region = STATE_EAST_BORNEO
				owns_entire_state_region = STATE_NORTH_BORNEO
			}
		}
	}
}

YOG = { # Yogyakarta

	# Mataram
	dynamic_country_name = {
		name = dyn_c_srk_union
		adjective = dyn_c_srk_union_adj
		
		is_main_tag_only = yes
		priority = 0
		
		trigger = {
			exists = scope:actor
			scope:actor = {
				owns_entire_state_region = STATE_CENTRAL_JAVA
			}
		}
	}
}